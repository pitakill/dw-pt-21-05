class Container {
  constructor(id, origin, destination) {
    this.id = id;
    this.origin = origin;
    this.destination = destination;
  }

  save() {
    // Save to database
  }
}

module.exports = Container;
